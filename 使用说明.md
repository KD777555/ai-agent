# AI Agent 使用说明

## 项目简介

一个支持 RAG 知识库问答和 Tool Calling 工具调用的 AI Agent 服务。上传文档后 AI 基于你的资料回答问题，也可以查天气、算数学、搜索互联网。

**技术栈：** FastAPI（后端）+ Next.js（前端）+ Chroma（向量库）+ DeepSeek（模型）

---

## 快速启动

### 1. 启动后端

```bash
C:\Users\lenovo\.workbuddy\binaries\python\envs\default\Scripts\python.exe agent-project\step0_basics\app.py
```

看到 `✅ 知识库已加载（N 个文档块）` 和 `Uvicorn running on http://0.0.0.0:8000` 即启动成功。

### 2. 启动前端（新开一个终端）

```bash
cd agent-project/frontend
npm run dev
```

看到 `Ready in XXXms` 和 `http://localhost:3000` 即启动成功。

### 3. 打开页面

浏览器访问 http://localhost:3000

---

## 通用模式（💬）

适合：上传文档后让 AI 基于文档回答问题。

**使用步骤：**
1. 保持默认的「通用」模式
2. 在左侧上传区拖拽或点击上传文件（支持 .md .txt .pdf）
3. 上传成功后知识库状态显示为绿色
4. 在下方输入问题，AI 会基于上传的文档回答

**示例问题：**
- "介绍一下这份文档的内容"
- "帮我总结一下重点"

---

## 求职助手模式（💼）

适合：简历分析、岗位匹配、模拟面试。

**使用步骤：**
1. 点击顶栏「求职助手」切换模式
2. 上传你的简历（PDF 或 .md 格式）
3. 在左侧「岗位描述」输入框中粘贴目标 JD
4. 点击快捷按钮或直接输入问题

**快捷按钮：**

| 按钮 | 作用 |
|------|------|
| 📊 分析匹配度 | 自动对比简历和 JD 给出匹配分析 |
| 🎯 模拟面试 | AI 扮演面试官根据你的简历提问 |
| ✏️ 优化简历 | 给出具体的修改建议 |

**示例问题：**
- "我的简历匹配这个岗位吗？哪里需要改进？"
- "帮我写一段项目描述"
- "模拟面试中我回答得怎么样？"

---

## 全部功能列表

### 知识库管理
- 上传 .md / .txt / .pdf 文件
- 自动切分、embedding、存入向量库
- 上传成功后有文件名和块数提示

### 对话交互
- 流式输出：AI 逐字生成回答
- 停止生成：点击红色 ■ 按钮或按 `Esc` 键
- 编辑消息：hover 最后一条用户消息，点击 ✏️ 编辑
- 复制回答：hover AI 回答，点击「复制」
- 重试：请求失败时显示「重试」按钮
- 清除对话：点击右下角「清除」按钮（需确认）
- 对话持久化：刷新页面不丢失，切换模式自动恢复

### 工具栏
- 查天气：问"北京今天多少度"
- 算数学：问"15 * 8 + 24 等于多少"
- 搜网页：问"2026年NBA总冠军是哪支球队"

### 快捷键

| 快捷键 | 作用 |
|--------|------|
| `/` | 聚焦输入框 |
| `Enter` | 发送消息 |
| `Shift + Enter` | 换行 |
| `Esc` | 停止生成 |

### 数据显示
- 侧边栏顶部：知识库块数 + 对话轮数（实时统计）
- 顶栏：模式切换按钮上显示对话数
- 连接状态：绿色（已连接）/ 黄色（连接中）/ 红色（断开）
- 页面标题：未读消息时显示 `(N) AI Agent`

---

## 常见问题

**Q：上传文件后 AI 回答不准确？**
A：文件内容会被切分成多个块，提问时检索最相关的块拼入 prompt。如果回答不准确，可以尝试更精确的提问方式，或检查文件内容是否清晰。

**Q：为什么搜索有时返回"未找到结果"？**
A：搜索功能使用 Bing 爬取，受网络环境影响。如果搜索失败，AI 会告知搜索不可用。

**Q：对话记录存在哪里？**
A：存在浏览器本地存储（localStorage），按模式分开保存（通用/求职助手各一份），清除浏览器数据会丢失。

**Q：服务启动失败？**
A：检查 .env 文件中的 API Key 是否正确，以及 DeepSeek 余额是否充足。

---

## 项目结构

```
agent-project/
├── step0_basics/
│   ├── app.py              # FastAPI 后端服务（核心入口）
│   ├── chat.py             # 终端对话脚本（最小 demo）
│   ├── static/index.html   # 旧版前端（功能备用）
│   └── .env                # API Key 配置
├── step1_rag/
│   ├── rag_build.py      # 知识库构建脚本
│   ├── rag_query.py      # RAG 查询脚本（终端 demo）
│   ├── data/             # 示例文档
│   └── chroma_db/        # 向量库持久化存储
├── step2_tools/
│   └── tool_agent.py       # Tool Calling 独立演示脚本
├── frontend/
│   ├── app/
│   │   ├── page.tsx        # 主页面
│   │   ├── layout.tsx      # 根布局
│   │   ├── globals.css     # 全局样式
│   │   └── components/
│   │       ├── Header.tsx  # 顶栏
│   │       ├── Sidebar.tsx # 侧边栏
│   │       ├── ChatArea.tsx# 对话区
│   │       └── Hero.tsx    # 首页引导
│   └── next.config.ts      # Next.js 配置（API 代理）
├── uploaded_data/           # 上传的文档自动存这里
└── interview_prep.md        # 面试准备手册
```
